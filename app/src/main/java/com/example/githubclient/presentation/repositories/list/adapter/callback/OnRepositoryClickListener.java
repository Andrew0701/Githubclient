package com.example.githubclient.presentation.repositories.list.adapter.callback;

import com.example.githubclient.data.remote.model.Repository;

public interface OnRepositoryClickListener {

    void onRepositoryClick(Repository repository);
}
